# =============================================================================
# VOE DOCS ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your values.
# Never commit .env to version control!

# -----------------------------------------------------------------------------
# AUTHENTICATION MODE
# -----------------------------------------------------------------------------
# 'public'  - All content is accessible without login
# 'private' - All content requires authentication
AUTH_MODE=public

# JWT secret for signing tokens (REQUIRED in private mode)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# -----------------------------------------------------------------------------
# SECURITY AUDIT LOGGING
# -----------------------------------------------------------------------------
# Path to write audit logs (optional, logs to console by default)
# All auth attempts are logged with IP, timestamp, and outcome
# AUTH_AUDIT_LOG=./logs/auth-audit.log

# -----------------------------------------------------------------------------
# AWS SES EMAIL CONFIGURATION
# -----------------------------------------------------------------------------
# Required for OTP authentication in private mode
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
EMAIL_FROM=no-reply@process.finance

# -----------------------------------------------------------------------------
# CONTENT DIRECTORIES
# -----------------------------------------------------------------------------
# Paths relative to project root (defaults shown)
# CONTENT_DIR=./content
# PRIVATE_DIR=./private

# -----------------------------------------------------------------------------
# ANALYTICS (OPTIONAL)
# -----------------------------------------------------------------------------
# Google Analytics 4 Measurement ID
# Leave empty to disable tracking (respects C-OPS-ANALYTICS-OPT-IN-009)
NUXT_PUBLIC_GTAG_ID=

# -----------------------------------------------------------------------------
# SITE METADATA
# -----------------------------------------------------------------------------
NUXT_PUBLIC_SITE_NAME=Voe Docs
NUXT_PUBLIC_SITE_DESCRIPTION=Voe builder documentation
NUXT_PUBLIC_SITE_URL=https://docs.runvoe.com

# -----------------------------------------------------------------------------
# GITHUB WEBHOOK (OPTIONAL)
# -----------------------------------------------------------------------------
# Secret for validating GitHub webhook payloads
# Generate with: openssl rand -hex 20
GITHUB_WEBHOOK_SECRET=your-webhook-secret
